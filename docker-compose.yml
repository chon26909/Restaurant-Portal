version: '3.8'

services:

  # Application
  web:
    container_name: restaurant-portal
    # build:
    #   dockerfile: Dockerfile
    #   context: "."
    ports:
      - 80:80
    expose:
      - 80

volumes:
  db_data:
